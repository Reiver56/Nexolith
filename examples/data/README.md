# Example data

This directory contains small, non-sensitive input datasets used by documented example pipelines.
Keep fixtures readable, deterministic, and safe to commit.

Add a matching pipeline under `examples/pipelines/` when introducing a new dataset.
`orders.csv` is the five-row input for the
[first-pipeline tutorial](../../FIRST_PIPELINE.md); do not change its expected rows without
updating the example verification.
