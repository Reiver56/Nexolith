name: enrichment_task1_stage
source:
  type: postgresql
  connection_url: ${FONOLINK_PIPELINE_DATABASE_URL}
  query_file: ../queries/task1_stage.sql
transformations: []
destination:
  type: postgresql
  connection_url: ${FONOLINK_PIPELINE_DATABASE_URL}
  table: cdr_stage1
  mode: replace
