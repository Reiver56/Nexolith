name: enrichment_task2_dedup_and_nulls
source:
  type: postgresql
  connection_url: ${FONOLINK_PIPELINE_DATABASE_URL}
  query_file: ../queries/task2_dedup_and_nulls.sql
transformations: []
destination:
  type: postgresql
  connection_url: ${FONOLINK_PIPELINE_DATABASE_URL}
  table: cdr_stage2
  mode: replace
