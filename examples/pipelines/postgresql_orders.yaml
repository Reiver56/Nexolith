name: postgresql_completed_orders

source:
  type: csv
  path: ../data/orders.csv

transformations:
  - type: filter
    column: status
    operator: equals
    value: completed

destination:
  type: postgresql
  connection_url: ${DATABASE_URL}
  table: completed_orders
  mode: replace
