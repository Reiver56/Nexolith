# Domain exceptions

Expected configuration, connector, and transformation failures use the exception classes in this
package. Messages may be shown by the CLI, so they must be actionable and safe for terminal output.

Do not include secrets, tokens, passwords, or complete authenticated connection URLs.
